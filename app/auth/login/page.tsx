import AuthForm from "@/components/auth-form"

const Auth = () => {
  return (
    <div>
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
      <AuthForm type={ 'login' } />
    </div>
  )
}

export default Auth